#include <iostream>

int main(int argc, char** argv) {
    std::cout << "[SimRL CLI] Training started..." << std::endl;

    // Parse config file (TBD)
    // Load environment and model
    // Run training loop

    std::cout << "[SimRL CLI] Training completed successfully." << std::endl;
    return 0;
}
