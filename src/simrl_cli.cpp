#include <iostream>

auto main(int argc, char **argv) -> int {
    std::cout << "[SimRL CLI] Training started...\n";

    // Parse config file (TBD)
    // Load environment and model
    // Run training loop

    std::cout << "[SimRL CLI] Training completed successfully.\n";
    return 0;
}
